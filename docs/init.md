
```bash
Usage: pyzettel init [OPTIONS]

  Initialize a new pyzettel configuration

Options:
  -c, --config-file TEXT          Path to the configuration file
  -z, --zettelkasten-dir TEXT     Path to the zettelkasten directory
  -i, --id-template TEXT          Template for generating zettel IDs
  -m, --mkdocs / -M, --no-mkdocs  Generate mkdocs configuration
  -p, --pyproject / -P, --no-pyproject
                                  Generate pyproject.toml
  --help                          Show this message and exit.
```